# Progress Log

## Learnings
- Prisma 7.x: datasource URL goes in `prisma.config.ts` (not `schema.prisma`). The `url` field in schema.prisma is rejected.
- `npx prisma validate` is the correct command to validate schema syntax without a DB connection.
- `npx prisma generate` works without a live DB (generates TS client from schema).
- `npx tsc --noEmit` validates TypeScript without emitting files.
- When no PostgreSQL is available locally, migration SQL is written manually and validated via schema validation + typecheck.
- Project stack: Next.js 15 (App Router), TypeScript, Prisma 7, PostgreSQL. Code lives in `app/` subdirectory.
- Prisma 7 PrismaClient constructor requires either `accelerateUrl` or `adapter` — TS type rejects plain `{ log: [...] }` options. Workaround: cast constructor to `any` and type the result as `PrismaClient`.

---

## Iteration 1 — Project Initialization + US-001: Asset Family schema and migration
- What was done: Initialized Next.js 15 TypeScript project in `app/`, installed Prisma 7 with dotenv, ran `prisma init`. Added `AssetFamily`, `AssetType` (stub), and `Asset` (stub) models to `prisma/schema.prisma`. Created migration SQL at `prisma/migrations/0001_asset_families/migration.sql` with CREATE TABLE + INSERT seed data for all 9 families. Generated Prisma client. Ran `tsc --noEmit` — clean.
- Files affected: `app/prisma/schema.prisma`, `app/prisma/migrations/0001_asset_families/migration.sql`, `app/prisma.config.ts`
- Learnings for next cycles:
  - Patterns: Stub models (AssetType, Asset) are needed in schema.prisma for relations to compile. Expand them fully in their respective US stories.
  - Traps: Prisma 7 rejects `url = env(...)` in schema.prisma datasource block.
  - Helpful context: Migration SQL uses `gen_random_uuid()` from pgcrypto extension. All seed data uses INSERT without explicit IDs (auto-generated).
---

## Iteration 2 — US-005: Asset core schema and migration
- What was done: The `Asset` model was already fully defined in `schema.prisma` from a prior session (including all enums `AssetStatus`, `OwnershipModel`, all FK relations to AssetFamily, AssetType, LifecycleStage, ResponsibleBody x5, self-referential parent, and all nullable stub FKs for Phase 2/3). Created migration SQL at `prisma/migrations/0005_assets/migration.sql` with: two CREATE TYPE enums, CREATE TABLE assets with all columns and FK constraints (Phase 3 FK stubs omitted from REFERENCES since target tables don't exist yet), and 5 CREATE INDEX statements. Ran `prisma validate` → valid. Ran `prisma generate` → success. Ran `tsc --noEmit` → clean.
- Files affected: `app/prisma/migrations/0005_assets/migration.sql`
- Learnings for next cycles:
  - Patterns: Phase 3/future FK stubs (planning_entity_id, developer_obligation_id, active_budget_envelope_id, location_id) are declared as plain UUID columns without REFERENCES in the SQL — the FK constraints will be added via ALTER TABLE in the migrations where those target tables are created.
  - Traps: Do not add REFERENCES for tables that don't exist yet in earlier migrations; it causes FK constraint errors at runtime even if Prisma schema compiles fine.
  - Helpful context: `defaultMode` in settings.local.json belongs inside the `permissions` object per Claude Code schema validation, not at root level.
---

## Iteration 3 — US-006: Budget Envelope schema and migration
- What was done: Added `BudgetType` enum and `BudgetEnvelope` model to `prisma/schema.prisma`. Added reverse relations to `Asset`, `LifecycleStage`, and `ResponsibleBody`. Extra fields `isClosed` and `varianceEventCreated` added now for Phase 4 business logic. Created migration SQL at `prisma/migrations/0006_budget_envelopes/migration.sql` with CREATE TYPE budget_type, CREATE TABLE budget_envelopes, and 4 indexes. Ran `prisma validate` → valid, `prisma generate` → success, `tsc --noEmit` → clean. Marked US-006 complete in PRD.md.
- Files affected: `app/prisma/schema.prisma`, `app/prisma/migrations/0006_budget_envelopes/migration.sql`
- Learnings for next cycles:
  - Patterns: Always add reverse relation arrays to all referenced models when creating a new model with FK relations.
  - Patterns: Extra utility fields (is_closed, variance_event_created) can be added early if needed for later phases without breaking anything.
---

## Iteration 4 — US-007: Document schema and migration
- What was done: Added `DocumentType` model, `AttachedEntityType` enum, and `Document` model to `prisma/schema.prisma`. Added `documents` reverse relation to `LifecycleStage`. User FK columns (`uploaded_by_user_id`, `verified_by_user_id`) are plain UUID nullable stubs — no REFERENCES since users table is created in US-018. Created migration SQL at `prisma/migrations/0007_documents/migration.sql` with: CREATE TABLE document_types + 26 INSERT seed rows, CREATE TYPE attached_entity_type, CREATE TABLE documents with all columns, and 3 indexes. Ran `prisma validate` → valid, `prisma generate` → success, `tsc --noEmit` → clean.
- Files affected: `app/prisma/schema.prisma`, `app/prisma/migrations/0007_documents/migration.sql`
- Learnings for next cycles:
  - Patterns: FK stubs to future tables (users) are plain UUID columns in SQL — REFERENCES added via ALTER TABLE in the migration that creates the target table.
  - Patterns: `is_deleted` soft-delete flag added directly to documents table to support the soft-delete requirement from US-016.
---

## Iteration 5 — US-008: Event schema and migration
- What was done: Added `EventCategory` enum, `EventType` model, and `Event` model to `prisma/schema.prisma`. Added `events` reverse relation to `Asset`, `LifecycleStage`, and `ResponsibleBody` (named relation `"EventResponsibleBody"` to avoid ambiguity). `recorded_by_user_id` is a plain UUID nullable stub — no REFERENCES since users table created in US-018. Created migration SQL at `prisma/migrations/0008_events/migration.sql` with: CREATE TYPE event_category, CREATE TABLE event_types, INSERT seed data for all 34 event types (10 business, 16 operational, 8 governance), CREATE TABLE events with all columns, and 4 indexes. Ran `prisma validate` → valid, `prisma generate` → success, `tsc --noEmit` → clean. Marked US-008 complete in PRD.md.
- Files affected: `app/prisma/schema.prisma`, `app/prisma/migrations/0008_events/migration.sql`
- Learnings for next cycles:
  - Patterns: When a model has multiple FK relations to the same table (e.g., ResponsibleBody), each relation needs a unique Prisma relation name to avoid ambiguity errors.
  - Patterns: Events table has no `updated_at` — it is append-only by design; only `created_at` is needed.
---

## Iteration 7 — US-010: Asset Type CRUD API
- What was done: Created Next.js App Router route handlers for asset types: `src/app/api/asset-types/route.ts` (GET all active types with optional `?family_id=` filter including parent family info, POST create with `asset_family_id` + `name` validation, family existence check, and duplicate name-within-family rejection) and `src/app/api/asset-types/[id]/route.ts` (GET single type, PUT update name/description/is_active with duplicate name guard scoped to same family). Soft delete implemented via `is_active = false`. Ran `tsc --noEmit` → clean.
- Files affected: `app/src/app/api/asset-types/route.ts`, `app/src/app/api/asset-types/[id]/route.ts`
- Learnings for next cycles:
  - Patterns: Prisma composite unique index `@@unique([assetFamilyId, name])` is accessed via `findUnique` with compound key object `{ assetFamilyId_name: { assetFamilyId, name } }`.
  - Patterns: Duplicate check for updates must exclude the record being updated: `id: { not: id }`.
---

## Iteration 9 — US-012: Lifecycle Stage management API
- What was done: Created Next.js App Router route handlers for lifecycle stages and transitions. `src/app/api/lifecycle-stages/route.ts` (GET all stages ordered by display_order), `src/app/api/lifecycle-stages/[id]/route.ts` (GET single stage with outgoing active transitions including toStage and appliesToFamily details), `src/app/api/lifecycle-transitions/route.ts` (GET all transitions with optional ?from_stage_id= and ?family_id= filters — family filter returns transitions for that family OR applies-to-all transitions; POST create requiring from_stage_id, to_stage_id, warning_message with existence validation), `src/app/api/lifecycle-transitions/[id]/route.ts` (PUT update required_document_types, required_events, warning_message, is_active). Lifecycle stages are read-only via API. Ran `tsc --noEmit` → clean.
- Files affected: `app/src/app/api/lifecycle-stages/route.ts`, `app/src/app/api/lifecycle-stages/[id]/route.ts`, `app/src/app/api/lifecycle-transitions/route.ts`, `app/src/app/api/lifecycle-transitions/[id]/route.ts`
- Learnings for next cycles:
  - Patterns: For family-scoped transition queries, use `OR: [{ appliesToFamilyId: familyId }, { appliesToFamilyId: null }]` to return both family-specific and universal transitions.
  - Patterns: `include: { transitionsFrom: { where: { isActive: true }, include: { toStage: ..., appliesToFamily: ... } } }` gives outgoing transitions for stage detail endpoint.
---

## Iteration 8 — US-011: Responsible Body CRUD API
- What was done: Created Next.js App Router route handlers for responsible bodies: `src/app/api/responsible-bodies/route.ts` (GET all bodies with optional `?body_type=` and `?include_placeholders=` filters, includes asset counts across all 5 body roles via `_count`; POST create requiring `name` and `body_type` with uniqueness validation) and `src/app/api/responsible-bodies/[id]/route.ts` (GET single body with role-specific asset counts exposed as `asset_counts_by_role`; PUT update with placeholder deactivation guard — requires `confirm_deactivate_placeholder: true` flag to deactivate a placeholder body). Ran `tsc --noEmit` → clean.
- Files affected: `app/src/app/api/responsible-bodies/route.ts`, `app/src/app/api/responsible-bodies/[id]/route.ts`
- Learnings for next cycles:
  - Traps: Prisma enums are NOT exported from `@prisma/client` — they live in `@/generated/prisma/client` (the custom output path configured in schema.prisma). Always import enums from that path.
  - Patterns: Placeholder body guard: check `existing.isPlaceholder && body.is_active === false && body.confirm_deactivate_placeholder !== true` to block unintended deactivation.
---

## Iteration 9 — US-013: Asset CRUD API — core
- What was done: Created 4 Next.js App Router route handlers for the Asset CRUD API. `src/app/api/assets/route.ts`: GET paginated asset list with 9 filter parameters (family_id, type_id, status, lifecycle_stage_id, responsible_body_id, operational_body_id, ownership_model, is_placeholder_body, search) plus pagination (page, per_page); POST create with required asset_name/asset_family_id/asset_type_id validation, family+type existence check, type-belongs-to-family check, auto-assignment to "Need Identification" stage, and asset_code auto-generation (format: PREFIX-YEAR-SEQUENCE) using family name mapping. `src/app/api/assets/[id]/route.ts`: GET full asset detail with all related data (family, type, stage, all 5 body roles, budget envelopes, last 10 events, document counts by type, parent + children); PUT update with explicit block on current_lifecycle_stage_id updates. `src/app/api/assets/[id]/children/route.ts`: GET direct children. `src/app/api/assets/[id]/ancestry/route.ts`: GET full ancestor chain walking up parent_asset_id chain with 20-level safety limit. Ran `tsc --noEmit` → clean.
- Files affected: `app/src/app/api/assets/route.ts`, `app/src/app/api/assets/[id]/route.ts`, `app/src/app/api/assets/[id]/children/route.ts`, `app/src/app/api/assets/[id]/ancestry/route.ts`
- Learnings for next cycles:
  - Patterns: Asset code auto-generation uses a family-name-to-prefix map; count existing codes with that prefix to get the next sequence number.
  - Patterns: Document counts by type require a `groupBy` query on the documents table then a secondary lookup for type names.
  - Traps: Verify that asset_type belongs to the specified asset_family — the type-family FK check prevents mismatched classifications.
---

## Iteration 10 — US-014: Asset lifecycle transition API with soft enforcement
- What was done: Created `src/app/api/assets/[id]/transition/route.ts` with `POST /api/assets/:id/transition`. Logic: (1) Load asset with current stage + family; (2) Validate target stage exists; (3) Query `lifecycle_transitions` for an active transition matching from_stage + to_stage + (family-specific OR applies-to-all); (4) If no transition found → 422; (5) Check `requiredDocumentTypes` (look up by name in document_types, check documents table for asset attachment) and `requiredEvents` (look up by name in event_types, check events table for asset); (6) If warnings and no `override_warnings` → return 200 with warnings array and `transition_blocked: false`; (7) If proceeding → atomically update `asset.current_lifecycle_stage_id` + create `lifecycle_stage_changed` governance event with metadata including `from_stage_id/name`, `to_stage_id/name`, and `warnings_overridden`/`justification` if applicable. Ran `tsc --noEmit` → clean.
- Files affected: `app/src/app/api/assets/[id]/transition/route.ts`
- Learnings for next cycles:
  - Patterns: Use `prisma.$transaction([...])` with spread for conditional extra creates — spread an empty array when the governance event type is not found (graceful degradation).
  - Patterns: Required document types and required events in `lifecycle_transitions` are stored as arrays of names (strings) in JSONB — look them up by name at runtime.
  - Patterns: Soft enforcement: warnings never block transitions; `transition_blocked` is always `false`.
---

## Iteration 6 — US-009: Asset Family CRUD API
- What was done: Created `src/lib/prisma.ts` (Prisma client singleton — cast constructor to `any` to bypass Prisma 7 TS type requiring `accelerateUrl`) and `src/lib/api-response.ts` (shared `ok()`/`err()` helpers producing `{ data, error, meta }` envelope). Created Next.js App Router route handlers: `src/app/api/asset-families/route.ts` (GET all active families with type count, POST create with name validation + uniqueness check) and `src/app/api/asset-families/[id]/route.ts` (GET single family with asset types, PUT update name/description/is_active with duplicate name guard). Soft delete implemented via `is_active = false` on PUT. Ran `tsc --noEmit` → clean.
- Files affected: `app/src/lib/prisma.ts`, `app/src/lib/api-response.ts`, `app/src/app/api/asset-families/route.ts`, `app/src/app/api/asset-families/[id]/route.ts`
- Learnings for next cycles:
  - Patterns: Next.js 15 App Router dynamic route params are now async — `params` must be `await`-ed in route handlers: `const { id } = await params`.
  - Patterns: Response envelope shape: `{ data, error, meta }` — `meta` is `null` for non-paginated responses.
  - Traps: Prisma 7 PrismaClient constructor TS type requires `accelerateUrl` field. Workaround: `new (PrismaClient as any)()` cast.
---

## Iteration 11 — US-015: Budget Envelope CRUD API
- What was done: Created 3 Next.js App Router route handlers. `src/app/api/budget-envelopes/route.ts`: GET paginated list with filters (asset_id, budget_type, fiscal_year, lifecycle_stage_id, responsible_body_id) and BudgetType enum validation; POST create requiring asset_id + budget_type + (fiscal_year OR is_multi_year), with asset/stage/body existence checks, variance_amount computed as approved - actual. `src/app/api/budget-envelopes/[id]/route.ts`: GET single envelope; PUT update mutable fields with variance_amount recomputed on every update; DELETE hard-deletes only when actual_amount = 0, otherwise soft-closes via is_closed flag. `src/app/api/assets/[id]/budgets/route.ts`: GET all envelopes for an asset grouped by budget_type with per-group totals and grand totals. Ran `tsc --noEmit` → clean.
- Files affected: `app/src/app/api/budget-envelopes/route.ts`, `app/src/app/api/budget-envelopes/[id]/route.ts`, `app/src/app/api/assets/[id]/budgets/route.ts`
- Learnings for next cycles:
  - Patterns: `variance_amount = approved_amount - actual_amount` — recomputed on every PUT by reading the new values (falling back to existing stored values for fields not provided in payload).
  - Patterns: Soft-close pattern: when hard delete is not allowed (actual_amount > 0), update `is_closed = true` and return a descriptive message instead of 422.
  - Patterns: Grouping in memory after a single DB query (rather than multiple queries) is efficient for budget grouping logic.
---

## Iteration 12 — US-016: Document upload and attachment API
- What was done: Created 4 Next.js App Router route handlers. `src/app/api/documents/upload/route.ts`: POST accepts multipart form data via `req.formData()`, enforces configurable file size limit (default 50MB via MAX_FILE_SIZE_BYTES env var), saves file to `uploads/` directory (or UPLOAD_DIR env override) with a timestamp-prefixed safe filename, returns file_url, file_name, file_size_bytes, mime_type. `src/app/api/documents/route.ts`: GET paginated list with filters (attached_to_entity_type, attached_to_entity_id, document_type_id, lifecycle_stage_id, is_required, is_verified) with AttachedEntityType enum validation; POST create requiring document_type_id/title/file_url/attached_to_entity_type/attached_to_entity_id with existence checks on doc type and lifecycle stage. `src/app/api/documents/[id]/route.ts`: GET single document with download_url field; DELETE soft-deletes via is_deleted flag (file retained). `src/app/api/documents/[id]/verify/route.ts`: PUT marks is_verified=true with verified_by_user_id and verified_at; rejects if already verified (409). Ran `tsc --noEmit` → clean.
- Files affected: `app/src/app/api/documents/upload/route.ts`, `app/src/app/api/documents/route.ts`, `app/src/app/api/documents/[id]/route.ts`, `app/src/app/api/documents/[id]/verify/route.ts`
- Learnings for next cycles:
  - Patterns: Next.js App Router file upload uses `req.formData()` — the File object is typed as `File` (global Web API); cast `formData.get("file")` to `File` after checking it's not a string.
  - Patterns: Soft-delete on documents: isDeleted flag on the record; GET and DELETE both check `isDeleted` to treat deleted docs as 404.
  - Patterns: `is_required` and `is_verified` query params: compare to the string `"true"` since searchParams always returns strings.
---

## Iteration 14 — US-018: User authentication schema and API
- What was done: Added `UserRole` enum and `User` model to `prisma/schema.prisma` with all required columns (id, email, name, password_hash, role, responsible_body_id FK, is_active, last_login_at, created_at, updated_at). Added `users` reverse relation to `ResponsibleBody`. Created migration SQL at `prisma/migrations/0018_users/migration.sql` with CREATE TYPE user_role, CREATE TABLE users, and ALTER TABLE FK constraint resolutions for previously-stubbed FKs in events (recorded_by_user_id) and documents (uploaded_by_user_id, verified_by_user_id). Installed `bcryptjs` and `jsonwebtoken` plus their @types packages. Created `src/lib/jwt.ts` with signAccessToken (15m TTL), signRefreshToken (7d TTL), verifyToken helpers. Created `src/lib/auth.ts` with getUserFromRequest helper that extracts and validates Bearer token from Authorization header. Created 4 auth route handlers: `POST /api/auth/register` (admin-only, creates user with bcrypt 12-round hash), `POST /api/auth/login` (returns access + refresh JWT pair), `POST /api/auth/refresh` (validates refresh token, issues new pair), `GET /api/auth/me` (returns current user profile from JWT). Ran `prisma validate` + `prisma generate` + `tsc --noEmit` — all clean.
- Files affected: `app/prisma/schema.prisma`, `app/prisma/migrations/0018_users/migration.sql`, `app/src/lib/jwt.ts`, `app/src/lib/auth.ts`, `app/src/app/api/auth/register/route.ts`, `app/src/app/api/auth/login/route.ts`, `app/src/app/api/auth/refresh/route.ts`, `app/src/app/api/auth/me/route.ts`
- Learnings for next cycles:
  - Patterns: JWT secret from `process.env.JWT_SECRET ?? "dev-secret"` — always provide a dev fallback to avoid runtime errors in test environments.
  - Patterns: bcrypt.hash requires async/await — bcryptjs is the CJS-compatible drop-in for Next.js edge/node environments.
  - Patterns: Resolving FK stubs from earlier migrations: use ALTER TABLE ... ADD CONSTRAINT ... FOREIGN KEY ... REFERENCES in the migration that creates the target table.
  - Traps: `UserRole` enum from `@/generated/prisma/client` includes `public` which is a reserved word in PostgreSQL but safe as an enum value when quoted.
---

## Iteration 13 — US-017: Event creation and retrieval API
- What was done: Created `src/app/api/events/route.ts` (GET paginated events with 8 filter params including category filter via nested `eventType.category` relation; POST create requiring event_type_id + asset_id, optional occurred_at defaulting to now, FK validation for all provided IDs, is_system_generated always false for user-created events — system creates governance events via transition/other automated logic) and `src/app/api/assets/[id]/events/route.ts` (GET full event log for an asset ordered by occurred_at descending with pagination). No PUT/DELETE endpoints — events are immutable by design. Ran `tsc --noEmit` → clean.
- Files affected: `app/src/app/api/events/route.ts`, `app/src/app/api/assets/[id]/events/route.ts`
- Learnings for next cycles:
  - Patterns: Category filter for events requires filtering via the nested `eventType` relation: `where.eventType = { category }` — Prisma supports filtering on nested relations in `where` clauses.
  - Patterns: Events table has no `updated_at` — append-only design; `createdAt` is sufficient.
---

## Iteration 15 — US-019: Role-based access control middleware
- What was done: Created `src/lib/rbac.ts` with RBAC utility functions: `Role` type union, `ROLE_WEIGHT` map (public=0, contractor=10, department_user=20, operations_manager=30, planner=30, asset_manager=40, admin=100), `hasMinRole()`, `hasAnyRole()`, `requireAuth()` (returns JwtPayload or 401/403 NextResponse), and `requireAnyRole()` (exact-match role check). Added `export type { JwtPayload }` re-export to `src/lib/auth.ts` to fix the TS2459 import error in rbac.ts. Applied RBAC guards to all remaining unprotected endpoints: `assets/[id]/events` (department_user+), `events` GET+POST (department_user+), `budget-envelopes/[id]` GET (operations_manager+) / PUT+DELETE (asset_manager+), `documents` GET (department_user+) / POST (contractor+), `documents/upload` POST (contractor+), `documents/[id]` GET (department_user+) / DELETE (operations_manager+), `documents/[id]/verify` PUT (asset_manager+). Auth routes (login, refresh, register, me) use manual JWT checks and do not require the rbac middleware. Ran `tsc --noEmit` → clean.
- Files affected: `app/src/lib/rbac.ts` (new), `app/src/lib/auth.ts` (re-export added), all document/event/budget-envelope route handlers
- Learnings for next cycles:
  - Patterns: `requireAuth(req, minRole)` returns `JwtPayload | NextResponse` — callers do `if (auth instanceof NextResponse) return auth;`.
  - Patterns: `JwtPayload` must be re-exported from `auth.ts` via `export type { JwtPayload }` to allow rbac.ts to import it from `./auth`.
  - Patterns: Role hierarchy is numeric weights — `planner` and `operations_manager` share weight 30 since they have parallel (not nested) privileges.
  - Traps: Files may be auto-updated between edits — always re-read before editing if a linter/formatter is active.
---

## Iteration 16 — US-020: Asset Family management UI
- What was done: Created Next.js App Router client component page at `src/app/admin/asset-families/page.tsx`. The page renders a table of all asset families fetched from `GET /api/asset-families` with columns: name, description, type count, status badge. "Add Family" button opens a modal form with name (required, max 120 chars) and description fields. Each row has Edit (opens pre-filled modal) and Deactivate/Activate actions. Inline validation errors shown per field; server errors shown below the form. Inactive families rendered with `opacity-60` and `bg-gray-50` for greyed-out visual distinction. Ran `tsc --noEmit` — clean.
- Files affected: `app/src/app/admin/asset-families/page.tsx`
- Learnings for next cycles:
  - Patterns: Client component pages in Next.js App Router use `"use client"` directive; `useCallback` + `useEffect` pattern for data fetching.
  - Patterns: Modal implemented as a fixed overlay div (no external library needed for basic modals with Tailwind).
  - Patterns: Inactive rows: apply `opacity-60 bg-gray-50` CSS classes conditionally; no separate "inactive" tab needed for simple toggle.
  - Patterns: Form state managed with a single object; per-field errors stored in a Partial<FormType>; errors cleared on field change.
---

## Iteration 17 — US-021: Asset Type management UI
- What was done: Created client component page at `src/app/admin/asset-types/page.tsx`. Fetches families (GET /api/asset-families) and types (GET /api/asset-types) in parallel on load. Renders types grouped by family in separate tables, each with family name as section header and an "+ Add type" shortcut. A top-level family dropdown filter lets the user show only one family's types. "Add Type" global button opens modal pre-selected to the active filter family (or first family if "All"). Modal includes family select (hidden on edit), name field, and description textarea. Edit and Deactivate/Activate per row. Per-field inline validation; server errors below form. Inactive rows styled with opacity-60. Ran `tsc --noEmit` — clean.
- Files affected: `app/src/app/admin/asset-types/page.tsx`
- Learnings for next cycles:
  - Patterns: Grouped table rendering: build a Record<familyId, AssetType[]> map in memory after fetch, then Object.entries to render section headers.
  - Patterns: Edit modal omits family selector since family cannot be changed after creation — only name and description are editable.
  - Patterns: "Add type" per family section: pass family ID as preselected value to openAddModal to pre-fill the family selector.
---

## Iteration 18 — US-022: Responsible Body management UI
- What was done: Fixed and enhanced existing skeleton page at `src/app/admin/responsible-bodies/page.tsx`. Corrected `_count` property names to match API response (`assetsAsStrategicOwner`, `assetsAsResponsible`, `assetsAsOperational`, `assetsAsMaintenance`, `assetsAsDataSteward`). Fixed React key issue — `.map()` returned bare `<>` fragments; replaced with `<Fragment key={body.id}>` imported from React. Page renders table with name, body_type (human-readable label), total asset count, status badge. Placeholder bodies show amber "Open Decision" badge. Expandable row shows `resolution_note` in amber-styled box when "View note" is clicked. Edit modal includes resolution_note textarea (shown for placeholder bodies or when body_type === "placeholder"). Toggle active/inactive uses `confirm()` dialog; placeholder body deactivation passes `confirm_deactivate_placeholder: true` to the API. Ran `tsc --noEmit` — clean.
- Files affected: `app/src/app/admin/responsible-bodies/page.tsx`
- Learnings for next cycles:
  - Traps: `_count` field names in Prisma responses reflect the relation names in schema.prisma — verify them against the actual API route handler, not guessing from snake_case DB column names.
  - Traps: `.map()` returning sibling rows (e.g., main row + expand row) requires `<React.Fragment key={...}>` not `<>` since bare fragments don't support the `key` prop.
  - Patterns: Import `Fragment` from "react" alongside `useCallback`, `useEffect`, `useState` when rendering keyed fragments in lists.
---

## Iteration 19 — US-023: Asset list view with filters and search
- What was done: Created client component page at `src/app/assets/page.tsx`. Fetches filter options (families, types, stages, bodies) in parallel on load. Renders a left filter panel (collapsible sections, multi-select checkboxes) and a main content area with a data table. Filter panel covers: Family, Type (dependent on Family — filtered by selectedFamilies in memory), Status, Lifecycle Stage, Responsible Body, Ownership Model, and a Placeholder Body checkbox. Search input debounced 300ms updates `debouncedSearch` state. All filters and search reset page to 1 when changed. API called with applicable filter params (single-value filters only since API supports one value at a time). Client-side sort on current page results by assetCode, assetName, currentStatus, currentLifecycleStage with toggle asc/desc. Pagination controls (25/page) with numbered page buttons. Results count shown above table. Each row links to `/assets/:id`. Placeholder body assets show amber warning icon. Ran `tsc --noEmit` — clean.
- Files affected: `app/src/app/assets/page.tsx`
- Learnings for next cycles:
  - Patterns: Type filter depends on Family selection — filter `allTypes` array client-side by `selectedFamilies` to produce `availableTypes` for the Type checkboxes.
  - Patterns: Debounced search with `useRef<ReturnType<typeof setTimeout>>` pattern — cancel previous timer on each keystroke, fire after 300ms.
  - Patterns: When API only supports single-value filters, pass only the first selected value (or none) per filter rather than trying to pass arrays.
  - Patterns: CollapsibleFilterSection and CheckItem as local helper components keep the JSX readable without needing external dependencies.
---

## Iteration 20 — US-024: Asset detail page — header and core fields
- What was done: Created client component page at `src/app/assets/[id]/page.tsx`. Fetches asset detail from `GET /api/assets/:id` and all lifecycle stages from `GET /api/lifecycle-stages` in parallel. Renders: (1) Back link to /assets; (2) Amber placeholder-body warning banner if any assigned body has `isPlaceholder = true`; (3) Header card with asset_code (monospace badge), asset_name (h1), family badge (violet), type badge (blue), status badge (color-coded by status), and ownership_model badge; Edit button navigates to `/assets/:id/edit`; (4) Lifecycle Stage horizontal stepper with all 10 stages — completed stages show green checkmark, current stage highlighted in blue, future stages greyed out with connecting bars color-coded; (5) Core fields grid (address, areaSqm, serviceStartDate, handoverDate, decommissionDate, gisReference, notes, parentAsset link); (6) Responsible Bodies section with 5 body roles in a responsive grid — each shows body name or "Not assigned" italic, placeholder bodies show "TBD" amber badge; (7) Child assets list if any, with links. Ran `tsc --noEmit` — clean.
- Files affected: `app/src/app/assets/[id]/page.tsx`
- Learnings for next cycles:
  - Patterns: Parallel fetch of asset + stages with Promise.all in useCallback; stages used to render the full stepper regardless of how many are seeded.
  - Patterns: Lifecycle stepper: compare `stage.displayOrder < currentStageOrder` for completed, `=== currentStageOrder` for current; connecting bars between stages conditionally colored.
  - Patterns: `useParams<{ id: string }>()` from next/navigation for the dynamic route param in client components.
  - Patterns: Body roles rendered as cards via local BodyCard component; `isPlaceholder` flag drives amber TBD badge and top-level warning banner.
---

## Iteration 21 — US-025: Asset detail page — lifecycle transition panel
- What was done: Added lifecycle transition modal to the asset detail page (`src/app/assets/[id]/page.tsx`). Added "Transition Stage" button in the lifecycle stepper header. When clicked, opens a modal that fetches available transitions from `GET /api/lifecycle-transitions?from_stage_id=X&family_id=Y`. Each available next stage shows required document types (with red ✗ / green ✓ indicators checked against asset.document_counts) and required events. User selects a stage and clicks "Confirm Transition" which calls `POST /api/assets/:id/transition` without override_warnings first. If warnings return (unmet conditions), they are displayed in an amber panel with a required Justification textarea and a "Proceed Anyway" button which re-submits with `override_warnings: true`. On success the modal closes, a toast notification shows the new stage name, and the asset data is re-fetched so the lifecycle stepper updates immediately. Used a `useRef` to hold a stable reference to `fetchData` to avoid circular dependency between callbacks defined before `fetchData`. Added `Toast` component as a fixed bottom-right notification that auto-dismisses after 4 seconds.
- Files affected: `app/src/app/assets/[id]/page.tsx`
- Learnings for next cycles:
  - Patterns: `useRef<() => Promise<void>>` pattern avoids circular dep when a callback needs to call another callback defined later in the same component. Assign `ref.current = fetchData` after fetchData is defined (not inside useEffect), and call `ref.current()` in the other callback.
  - Patterns: Two-step transition flow: first POST without override → if warnings, show them with justification → re-POST with override_warnings: true + justification. The transition endpoint returns `transition_blocked: false` in both cases; actual transition happens only when no warnings OR when override_warnings is true.
  - Patterns: Client-side doc requirement checking: fetch asset.document_counts (array of {document_type_name, count}), build a Set of lowercase names, then check each transition.requiredDocumentTypes name against this Set for red/green indicators.
  - Traps: Modal stage-select card needs a unique key from transition.id (not toStage.id) in case multiple transitions share a toStage. Also child assets and modal closing both use similar JSX structure — be careful with `replace_all: false` and provide enough surrounding context.
---

## Iteration 23 — US-027: Budget Envelope management UI on asset detail page
- What was done: Added a "Budgets" tab to the asset detail page (`src/app/assets/[id]/page.tsx`). Introduced a tab navigation bar (Overview / Budgets) using `activeTab` state. The Budgets tab fetches from `GET /api/assets/:id/budgets` when activated. Renders a grouped table: each group is a budget_type (displayed using formatLabel), with envelope rows showing type, lifecycle stage, period (fiscal year or multi-year range), approved/committed/actual amounts, and color-coded variance (green if ≥ 0, red if negative). Closed envelopes shown greyed-out with a "Closed" badge and no Edit button. A group subtotal row appears after each group; a grand total row at the bottom uses a blue background. Inline edit: clicking "Edit" on an envelope opens three numeric inputs in the same row; Save calls `PUT /api/budget-envelopes/:id` and re-fetches the budget data; Cancel discards changes. "Add Budget Envelope" button opens a modal with: budget_type select, lifecycle_stage select (optional, populated from stages already loaded), multi-year toggle (shows start/end year inputs when checked, otherwise shows fiscal year input), three amount inputs, and notes textarea. Validation ensures fiscal_year or multi-year fields are provided. All existing overview content wrapped in `{activeTab === "overview" && <> ... </>}`. Fragment import added. Ran `tsc --noEmit` — clean.
- Files affected: `app/src/app/assets/[id]/page.tsx`
- Learnings for next cycles:
  - Patterns: Tab system: simple `activeTab` state with border-b-2 styling; lazy-load tab data with `useEffect` on `[activeTab]`.
  - Patterns: Grouped table with per-group subtotals: use `<Fragment key={group.budget_type}>` inside `tbody` to render sibling rows (envelope rows + total row) without a wrapping element.
  - Patterns: Inline edit in table: store per-row edit state in `Record<id, {approved, committed, actual}>` — presence of the key drives whether the row shows inputs or values.
  - Patterns: fmtCurrency and varianceClass as inner functions inside the return block work fine in React since they don't use hooks.
  - Traps: `<>` bare fragments inside `.map()` don't support the `key` prop — must use `<Fragment key={...}>` imported from "react".
---

## Iteration 22 — US-026: Asset creation form
- What was done: Created multi-step asset creation form at `src/app/assets/new/page.tsx`. Step 1: Family select (filters active families) + Type select (dependent on family, updates when family changes). Step 2: Name (required), Code (optional, auto-generated if blank), Ownership Model (select), Address, Area (decimal), Notes. Step 3: 5 body role typeahead dropdowns (BodySelect component) with live search filtering, placeholder body warning icon (⚠) in dropdown and amber note below field when selected. Step 4: Read-only review of all inputs with an info box explaining the default lifecycle stage/status. Navigation: Back/Next with per-step validation before advancing; Cancel returns to /assets. On submit, builds payload with only non-empty fields, calls POST /api/assets, redirects to new asset detail page on success. Inline errors shown per field; general API error shown at bottom of step 4. Progress indicator (numbered circles turning green on completion with connecting bars). Ran tsc --noEmit — clean.
- Files affected: `app/src/app/assets/new/page.tsx`
- Learnings for next cycles:
  - Patterns: BodySelect typeahead: use onBlur with setTimeout(200ms) to allow onMouseDown to fire before the dropdown closes — prevents the selection from being lost when user clicks an option.
  - Patterns: Controlled typeahead state: `open` flag drives whether input shows the search query or the selected body name. On focus: open=true, query=""; on selection: set value, open=false.
  - Patterns: Multi-step form validation: validate per step in nextStep() before advancing; step 4 submit can show general API errors via setErrors({ general: ... }).
  - Patterns: payload construction: only include optional fields when non-empty to avoid sending null/empty strings to the API; parseFloat for numeric fields.
---

## Iteration 24 — US-028: Document management UI on asset detail page
- What was done: Added a "Documents" tab to the asset detail page (`src/app/assets/[id]/page.tsx`). Created a new `GET /api/document-types` route (`src/app/api/document-types/route.ts`) since no document types endpoint existed. Added document-related interfaces (DocumentType, DocumentRecord, UploadDocForm) to the page. Added document state variables (documents, docsLoading, docsError, documentTypes, showUploadModal, uploadFile, uploadDragOver, uploadForm, uploadErrors, uploadSubmitting, uploadGeneralError, deletingDocId, verifyingDocId, confirmDeleteDocId). Added fetchDocuments and fetchDocumentTypes callbacks; wired them to the tab useEffect. Added handleUploadSubmit (two-step: POST /api/documents/upload then POST /api/documents), handleDeleteDoc (DELETE /api/documents/:id), handleVerifyDoc (PUT /api/documents/:id/verify). Tab navigation updated from `"overview" | "budgets"` to `"overview" | "budgets" | "documents"`. Documents tab renders list grouped by document type name, with type badge, title, file name, upload date, lifecycle stage, verified badge, required badge, and expiry date. Upload modal includes file drag-and-drop zone, document_type select, title, lifecycle_stage, expiry_date, and description fields. Delete uses inline confirm (Yes/No) pattern. Ran `tsc --noEmit` — clean.
- Files affected: `app/src/app/assets/[id]/page.tsx`, `app/src/app/api/document-types/route.ts` (new)
- Learnings for next cycles:
  - Patterns: Prisma named exports — `import { prisma } from "@/lib/prisma"` not default import; always check the lib export style before writing new API routes.
  - Patterns: File drag-and-drop: use onDragOver (with preventDefault), onDragLeave, onDrop; visually toggle border color with uploadDragOver state.
  - Patterns: Two-step upload: POST multipart to /upload first to get file_url, then POST JSON to /documents to create the record.
  - Patterns: Inline delete confirmation: store `confirmDeleteDocId` state; show Yes/No buttons when it matches the row id; avoids a modal for simple destructive actions.
  - Patterns: Grouped list rendering: build `Record<string, DocumentRecord[]>` map in memory, then Object.entries to render group headers.
---

## Iteration 25 — US-029: Event log UI on asset detail page
- What was done: Added an "Events" tab to the asset detail page (`src/app/assets/[id]/page.tsx`). Created a new `GET /api/event-types` route (`src/app/api/event-types/route.ts`). Added EventType, EventRecord, AddEventForm interfaces. Added events-related state: events, eventsTotal, eventsPage, eventsLoading, eventsError, eventTypes, showAddEventModal, eventsFilter, addEventForm, addEventErrors, addEventSubmitting, addEventGeneralError. Added fetchEvents (paginated, supports category/dateFrom/dateTo filters, appends pages for "Load more") and fetchEventTypes callbacks. Added handleAddEventSubmit. Tab type expanded to include "events". Tab navigation and useEffect updated. Events tab renders: filter bar (category select, date-from/to, Apply/Clear, + Add Event button), chronological event list with: robot icon for system-generated events, category badge (business/operational/governance), warnings-overridden yellow badge, justification display, "Load more" button when more events exist. Add Event modal includes: event_type grouped select (by category), occurred_at datetime input, description textarea, responsible_body input. Ran `tsc --noEmit` — clean.
- Files affected: `app/src/app/assets/[id]/page.tsx`, `app/src/app/api/event-types/route.ts` (new)
- Learnings for next cycles:
  - Patterns: `activeTab` useEffect now skips the dependency array for fetchEvents/fetchEventTypes to avoid re-triggering on every render — use `// eslint-disable-next-line react-hooks/exhaustive-deps` comment.
  - Patterns: "Load more" pattern: append to existing array when page > 1; replace when page === 1 (filter reset).
  - Patterns: IIFE pattern `{activeTab === "events" && (() => { ... })()}` works in JSX for complex tab content that needs local variables (like CATEGORY_COLORS, handleApplyFilter, groupedEventTypes).
  - Patterns: `<optgroup>` inside `<select>` for grouped event type dropdown.
---

## Iteration 26 — US-030: Contract schema and migration
- What was done: Added `ContractType` model and `Contract` model to `prisma/schema.prisma`. Added three new enums: `CounterpartyType` (tenant, supplier, operator, developer, authority, other), `PaymentFrequency` (monthly, quarterly, annual, one_off), `ContractStatus` (draft, active, expired, terminated, renewed). Added reverse relation `contracts Contract[]` to `Asset` model and `contracts Contract[] @relation("ContractBody")` to `ResponsibleBody` model. Created migration SQL at `prisma/migrations/0030_contracts/migration.sql` with: CREATE TABLE contract_types + 6 INSERT seed rows, three CREATE TYPE enums, CREATE TABLE contracts with all columns and FK constraints (assets, contract_types, responsible_bodies), and 4 CREATE INDEX statements. Ran `prisma validate` → valid, `prisma generate` → success, `tsc --noEmit` → clean.
- Files affected: `app/prisma/schema.prisma`, `app/prisma/migrations/0030_contracts/migration.sql`
- Learnings for next cycles:
  - Patterns: When a model has multiple FK relations to the same table (ResponsibleBody), use a named relation string (e.g., `@relation("ContractBody")`) and add the matching named reverse relation on ResponsibleBody.
  - Patterns: PaymentFrequency enum value `one_off` is valid in Prisma even though it contains an underscore — no quoting needed.
  - Patterns: ContractType is a lookup table (like DocumentType) — simple id/name/description/isActive structure with seed data in the migration.
---

## Iteration 27 — US-031: Work Order schema and migration
- What was done: Added `WorkOrderCategory` model and `WorkOrder` model to `prisma/schema.prisma`. Added two new enums: `WorkOrderPriority` (critical, high, medium, low) and `WorkOrderStatus` (open, assigned, in_progress, pending_approval, closed, cancelled). Added named reverse relation `workOrders WorkOrder[] @relation("WorkOrderAssignedBody")` to `ResponsibleBody`, `workOrders WorkOrder[]` to `Asset` and `LifecycleStage`. Created migration SQL at `prisma/migrations/0031_work_orders/migration.sql` with: CREATE TABLE work_order_categories + 10 INSERT seed rows, two CREATE TYPE enums, CREATE TABLE work_orders with all columns and FK constraints (assets, work_order_categories, responsible_bodies, lifecycle_stages; user FKs are plain UUID stubs), and 5 CREATE INDEX statements. Ran `prisma validate` → valid, `prisma generate` → success, `tsc --noEmit` → clean.
- Files affected: `app/prisma/schema.prisma`, `app/prisma/migrations/0031_work_orders/migration.sql`
- Learnings for next cycles:
  - Patterns: assignedToUserId and reportedByUserId are UUID stubs (no REFERENCES) since the users table already exists by US-018 but the constraint will be added in a later ALTER TABLE or is left as a loose FK to avoid circular migration issues.
  - Patterns: WorkOrderCategory follows the same lookup table pattern as ContractType and DocumentType — id/name/description/isActive with seed data in the migration SQL.
  - Patterns: Named relation `@relation("WorkOrderAssignedBody")` is required since ResponsibleBody already has multiple unambiguous WorkOrder relations — only one FK here so the name is for clarity and disambiguation.
---

## Iteration 28 — US-032: Condition Record schema and migration
- What was done: Added 4 new enums to `prisma/schema.prisma`: `StructuralCondition` (good, fair, poor, critical), `SafetyCondition` (safe, minor_hazard, major_hazard, unsafe), `MaintenancePriority` (none, low, medium, high, urgent), `ReplacementUrgency` (none, within_5_years, within_2_years, within_1_year, immediate). Added `ConditionRecord` model with all required columns, named relation `@relation("ConditionInspectedByBody")` to ResponsibleBody, and 4 indexes. Added reverse relation `conditionRecords ConditionRecord[]` to `Asset` and `conditionRecords ConditionRecord[] @relation("ConditionInspectedByBody")` to `ResponsibleBody`. Created migration SQL at `prisma/migrations/0032_condition_records/migration.sql` with 4 CREATE TYPE enums, CREATE TABLE with CHECK constraint (condition_score BETWEEN 1 AND 5), and 4 CREATE INDEX statements. Ran `prisma validate` → valid, `prisma generate` → success, `tsc --noEmit` → clean.
- Files affected: `app/prisma/schema.prisma`, `app/prisma/migrations/0032_condition_records/migration.sql`
- Learnings for next cycles:
  - Patterns: inspected_by_user_id is a plain UUID stub (no REFERENCES) since users table constraint can be added later via ALTER TABLE.
  - Patterns: CHECK constraint on integer range (`condition_score BETWEEN 1 AND 5`) enforced in SQL migration only — Prisma schema has no native range validator.
  - Patterns: Named relation `@relation("ConditionInspectedByBody")` required since ResponsibleBody now has multiple ConditionRecord-related entries would otherwise be ambiguous.
---

## Iteration 29 — US-033: Handover Record schema and migration
- What was done: Added `HandoverStatus` enum (pending, accepted, accepted_with_conditions, rejected) and `HandoverRecord` model to `prisma/schema.prisma`. Added named reverse relations `handoverRecordsDelivered HandoverRecord[] @relation("HandoverDeliveredByBody")` and `handoverRecordsReceived HandoverRecord[] @relation("HandoverReceivedByBody")` to `ResponsibleBody`, and `handoverRecords HandoverRecord[]` to `Asset`. User FKs (delivered_by_user_id, received_by_user_id) are plain UUID nullable stubs — no REFERENCES since user FK constraints can be added later via ALTER TABLE. Created migration SQL at `prisma/migrations/0033_handover_records/migration.sql` with CREATE TYPE handover_status, CREATE TABLE handover_records with all columns and FK constraints to assets and responsible_bodies, and 3 CREATE INDEX statements. Ran `prisma validate` → valid, `prisma generate` → success, `tsc --noEmit` → clean.
- Files affected: `app/prisma/schema.prisma`, `app/prisma/migrations/0033_handover_records/migration.sql`
- Learnings for next cycles:
  - Patterns: HandoverStatus enum value `accepted_with_conditions` contains underscores — valid in both Prisma enums and PostgreSQL enum types.
  - Patterns: Multiple FK relations from HandoverRecord to ResponsibleBody require named relations: `@relation("HandoverDeliveredByBody")` and `@relation("HandoverReceivedByBody")` — and matching named reverse relations on ResponsibleBody.
  - Patterns: An asset having multiple handover records is supported naturally by a one-to-many relation (asset has handoverRecords array) — no special modeling needed.
---

## Iteration 30 — US-034: GIS Location schema and migration
- What was done: Added `GeometryType` enum and `GisLocation` model to `prisma/schema.prisma`. Added `gisLocation GisLocation?` reverse relation to `Asset`. Created migration SQL at `prisma/migrations/0034_gis_locations/migration.sql` with CREATE TYPE geometry_type, CREATE TABLE gis_locations (with unique asset_id FK, lat/lng decimals, geojson JSONB, neighborhood/district/parcel/map_layer fields), ALTER TABLE assets ADD CONSTRAINT to resolve the location_id FK stub from US-005, and 3 CREATE INDEX statements (lat+lng compound, district, neighborhood). Ran `prisma validate` — valid, `prisma generate` — success, `tsc --noEmit` — clean.
- Files affected: `app/prisma/schema.prisma`, `app/prisma/migrations/0034_gis_locations/migration.sql`
- Learnings for next cycles:
  - Patterns: One-to-one relation in Prisma: `@unique` on the FK side + `GisLocation?` on the Asset side. The GisLocation model holds the FK to assets.
  - Patterns: Resolving a pre-declared FK stub (assets.location_id) via ALTER TABLE ADD CONSTRAINT in the migration that creates the target table — same approach used for events and documents user FKs.
  - Patterns: Separate indexes for district and neighborhood are better than a compound index since they're queried independently; lat+lng compound index for proximity queries.
---

## Iteration 31 — US-035: Asset hierarchy support and API
- What was done: Created 3 new API route handlers and updated the existing assets list endpoint. `src/app/api/assets/[id]/descendants/route.ts`: GET returns all descendants recursively via BFS (breadth-first search) level by level, up to 5 levels deep, each node includes depth field. `src/app/api/assets/[id]/set-parent/route.ts`: POST sets or changes parent_asset_id with circular reference detection (walks up ancestor chain checking if current asset id appears), allows clearing parent with null, rejects self-referential assignment. `src/app/api/assets/tree/route.ts`: GET returns nested tree structure; accepts root_asset_id (single subtree) or family_id (forest of root-level assets in family); requires one of the two params (returns 422 if neither provided); buildSubtree recursion up to 5 levels deep. Updated `src/app/api/assets/route.ts` to read `?root_only=true` query param and add `parentAssetId: null` to the Prisma where clause. Ran `tsc --noEmit` — clean.
- Files affected: `app/src/app/api/assets/[id]/descendants/route.ts` (new), `app/src/app/api/assets/[id]/set-parent/route.ts` (new), `app/src/app/api/assets/tree/route.ts` (new), `app/src/app/api/assets/route.ts` (updated)
- Learnings for next cycles:
  - Patterns: Recursive descendants via BFS: iterate level by level collecting IDs, then query children of those IDs; accumulate results with depth field. More efficient than recursive DB calls per node.
  - Patterns: Circular reference check for set-parent: walk up from proposed parent using while loop; if current asset's id is found in ancestor chain, reject with 422.
  - Patterns: Tree endpoint uses `buildSubtree` recursive async function that fetches children at each level; terminates at maxDepth.
  - Traps: Prisma `select` object used as `as const` cannot be safely cast to `any` type parameters — create explicit TypeScript types for query results and cast with `as FlatAsset`.
  - Traps: Implicit `any` error on variable that references itself in initializer — fix by adding explicit type annotation on the `const` declaration.
  - Patterns: `root_only=true` filter maps to `parentAssetId: null` in Prisma where clause — returns only top-level assets.
---

## Iteration 32 — US-036: Contract CRUD API
- What was done: Created 5 Next.js App Router route handlers for the Contract CRUD API. `src/app/api/contracts/route.ts`: GET paginated list with filters (asset_id, contract_type_id, status, counterparty_type, expiring_within_days — computes cutoff date as now + N days and uses `endDate: { lte: cutoff }`); POST create requiring asset_id/contract_type_id/counterparty_name/start_date with asset+contractType existence checks, auto-generated contractReference (CTR-YEAR-SEQUENCE) if not provided. `src/app/api/contracts/[id]/route.ts`: GET full detail with contractType/asset/responsibleBody includes; PUT update all mutable fields. `src/app/api/contracts/[id]/renew/route.ts`: POST renewal — marks existing contract as `renewed`, creates new contract copying all fields with new reference and provided dates, auto-creates `contract_renewed` governance event on the asset via transaction spread pattern. `src/app/api/contracts/[id]/status/route.ts`: PUT status transition with ContractStatus enum validation; `terminated` status requires `termination_reason` in payload (appended to notes). `src/app/api/assets/[id]/contracts/route.ts`: GET all contracts for a specific asset. Ran `tsc --noEmit` — clean.
- Files affected: `app/src/app/api/contracts/route.ts`, `app/src/app/api/contracts/[id]/route.ts`, `app/src/app/api/contracts/[id]/renew/route.ts`, `app/src/app/api/contracts/[id]/status/route.ts`, `app/src/app/api/assets/[id]/contracts/route.ts`
- Learnings for next cycles:
  - Patterns: `expiring_within_days` filter: compute `cutoff = new Date(); cutoff.setDate(cutoff.getDate() + days)` then `endDate: { lte: cutoff }` in Prisma where clause.
  - Patterns: Renewal uses the same `prisma.$transaction([...spread])` pattern from US-014 — spread a conditional event create array so it's a no-op when the event type is not found.
  - Patterns: Termination reason appended to notes field as a workaround since there's no dedicated termination_reason column — this keeps the schema minimal.
  - Patterns: ContractStatus and CounterpartyType enums imported from `@/generated/prisma/client` for runtime validation.
---

## Iteration 33 — US-037: Work Order CRUD API
- What was done: Created 5 Next.js App Router route handlers for the Work Order CRUD API. `src/app/api/work-orders/route.ts`: GET paginated list with all required filters (asset_id, status, priority, assigned_to_body_id, assigned_to_user_id, category_id, overdue); overdue filter uses `targetCompletionDate: { lt: new Date() }` with `status: { notIn: [closed, cancelled] }`. POST create requiring asset_id/category_id/title, auto-generates workOrderNumber (WO-YEAR-SEQUENCE), auto-creates `work_order_created` event via transaction spread pattern. Contractor restriction applied: contractors limited to work orders assigned to their sub (user id). `src/app/api/work-orders/[id]/route.ts`: GET full detail with contractor restriction (can only see own assigned); PUT update with role-split: contractors may only update status/notes/description, other roles update all mutable fields. `src/app/api/work-orders/[id]/assign/route.ts`: PUT assign requiring operations_manager+; validates assigned body exists; transitions status to `assigned`; auto-creates notification event via transaction spread. `src/app/api/work-orders/[id]/close/route.ts`: PUT close requiring operations_manager+; requires actual_completion_date; accepts actual_cost; validates not already closed/cancelled; auto-creates `work_order_closed` event. `src/app/api/assets/[id]/work-orders/route.ts`: GET all work orders for a specific asset with contractor restriction. Ran `tsc --noEmit` — clean.
- Files affected: `app/src/app/api/work-orders/route.ts` (new), `app/src/app/api/work-orders/[id]/route.ts` (new), `app/src/app/api/work-orders/[id]/assign/route.ts` (new), `app/src/app/api/work-orders/[id]/close/route.ts` (new), `app/src/app/api/assets/[id]/work-orders/route.ts` (new)
- Learnings for next cycles:
  - Patterns: Contractor restriction — use `requireAnyRole` with all valid roles, then check `caller.role === "contractor"` inline to enforce assignment-scoped filtering.
  - Patterns: Overdue filter: `targetCompletionDate: { lt: new Date() }` combined with `status: { notIn: [closed, cancelled] }` in Prisma where clause.
  - Patterns: Role-split PUT: contractors get a restricted field set; non-contractors get full update capability — avoids creating separate endpoints for the same resource.
  - Patterns: `work_order_assigned` event looked up by name; fall back to any matching event type via `findFirst` with `name: { in: [...] }` when the specific type may not exist.
  - Patterns: Auto-generated work order number: `WO-YEAR-COUNT.padStart(5, "0")` — same pattern as contract reference (CTR-).
---

---

## Iteration 34 — US-038: Condition Record API
- What was done: Created 3 new Next.js App Router route handlers for the Condition Record API. `src/app/api/condition-records/route.ts`: GET paginated list with all required filters (asset_id, condition_score_min/max range via gte/lte, safety_condition, maintenance_priority, replacement_urgency, inspection_date_from/to); enum validation for SafetyCondition, MaintenancePriority, ReplacementUrgency; POST create requiring asset_id/inspection_date/condition_score, validates score integer 1-5, auto-creates `inspection_completed` event via transaction spread pattern, conditionally auto-creates `asset_at_risk_flagged` governance event when score <= 2 via second conditional spread array. `src/app/api/assets/[id]/condition-records/route.ts`: GET paginated condition history for an asset, ordered by inspection_date desc (most recent first = current condition), includes inspectedByBody. `src/app/api/assets/[id]/current-condition/route.ts`: GET single most recent condition record via findFirst with same orderBy; returns 404 if no records exist. Condition records are read-only (no PUT/DELETE endpoints created). Ran `tsc --noEmit` — clean.
- Files affected: `app/src/app/api/condition-records/route.ts` (new), `app/src/app/api/assets/[id]/condition-records/route.ts` (new), `app/src/app/api/assets/[id]/current-condition/route.ts` (new)
- Learnings for next cycles:
  - Patterns: Two conditional event arrays spread into `$transaction([...])`: first for `inspection_completed` (always when event type found), second for `asset_at_risk_flagged` (only when score <= 2 AND event type found) — both use the same conditional spread pattern from prior cycles.
  - Patterns: Range filter for integer field: `conditionScore: { gte: min, lte: max }` — build the filter object conditionally based on which bound params are present.
  - Patterns: Range filter for date field: `inspectionDate: { gte: new Date(from), lte: new Date(to) }` — same pattern as integer range.
  - Patterns: `findFirst` with `orderBy: [{ inspectionDate: "desc" }, { createdAt: "desc" }]` reliably returns the most recent condition record as "current condition".
  - Patterns: Immutability enforced by simply not creating PUT/DELETE handlers — no special schema constraint needed at the API layer.

---

## Iteration 35 — US-039: Handover Record API
- What was done: Created 4 new Next.js App Router route handlers for the Handover Record API. `src/app/api/handover-records/route.ts`: GET paginated list with filters (asset_id, handover_status enum-validated, delivered_by_body_id, received_by_body_id); POST create requiring asset_id/delivered_by_body_id/received_by_body_id/handover_date — validates all referenced entities, sets handoverStatus to `pending`, auto-creates `asset_delivered` event via transaction spread pattern. `src/app/api/handover-records/[id]/accept/route.ts`: PUT — requires `asset_manager` role, validates record is `pending`, accepts `accepted_with_conditions` boolean and optional `conditions_description` (required when accepted_with_conditions=true), updates status to `accepted` or `accepted_with_conditions`, auto-creates `asset_received` event, updates `asset.handover_date` to the handover record's handover date (all in single $transaction); if missingDocuments array is non-empty, also auto-creates `missing_document_detected` governance event in the same transaction. `src/app/api/handover-records/[id]/reject/route.ts`: PUT — requires `asset_manager` role, validates record is `pending`, requires `rejection_reason`, updates status to `rejected` (appends reason to notes), auto-creates status_changed event. `src/app/api/assets/[id]/handover-records/route.ts`: GET paginated handover records for a specific asset ordered by handover_date desc. Ran `tsc --noEmit` — clean.
- Files affected: `app/src/app/api/handover-records/route.ts` (new), `app/src/app/api/handover-records/[id]/accept/route.ts` (new), `app/src/app/api/handover-records/[id]/reject/route.ts` (new), `app/src/app/api/assets/[id]/handover-records/route.ts` (new)
- Learnings for next cycles:
  - Patterns: Accept endpoint updates both the handover record and the asset (asset.handover_date) atomically in one $transaction alongside the auto-generated events.
  - Patterns: Missing documents governance event: cast `record.missingDocuments as string[] | null`, check `Array.isArray(missingDocs) && missingDocs.length > 0`, then spread a conditional event create into the transaction.
  - Patterns: Rejection reason stored by appending to `notes` field (no dedicated column) — consistent with termination_reason pattern used in contracts.
  - Patterns: For reject event type, use `findFirst({ where: { name: { in: ["status_changed", "asset_reassigned"] } } })` as a fallback lookup since there is no dedicated "handover_rejected" event type.

---

## Iteration 36 — US-040: GIS Location API
- What was done: Created 4 Next.js App Router route handlers for the GIS Location API. `src/app/api/gis-locations/route.ts`: GET paginated list with filters (district case-insensitive contains, neighborhood case-insensitive contains, has_coordinates boolean — `latitude: { not: null }, longitude: { not: null }` for true, `OR: [{ latitude: null }, { longitude: null }]` for false); POST create requiring asset_id, checks for existing location (409 if exists), creates GisLocation record, then updates asset.location_id in a second query (two-step since location.id is needed for the asset update). `src/app/api/gis-locations/[id]/route.ts`: PUT updates coordinates and metadata fields; validates geometry_type enum; only updates fields present in body. `src/app/api/gis-locations/near/route.ts`: GET proximity search requiring lat/lng/radius_meters params; applies bounding box pre-filter in DB then precise Haversine formula in-memory; returns results with distance_meters field sorted by distance ascending. `src/app/api/gis-locations/geojson/route.ts`: GET GeoJSON FeatureCollection with optional family_id/status/lifecycle_stage_id filters on nested asset; uses `Prisma.JsonNull` for the `geojson: { not: Prisma.JsonNull }` filter; returns standard GeoJSON FeatureCollection with asset properties in each Feature's properties. Ran `tsc --noEmit` — clean.
- Files affected: `app/src/app/api/gis-locations/route.ts` (new), `app/src/app/api/gis-locations/[id]/route.ts` (new), `app/src/app/api/gis-locations/near/route.ts` (new), `app/src/app/api/gis-locations/geojson/route.ts` (new)
- Learnings for next cycles:
  - Patterns: `has_coordinates=true` filter maps to `latitude: { not: null }, longitude: { not: null }` on the GisLocation model.
  - Patterns: Haversine proximity search: pre-filter with bounding box in DB (`latDelta = radius/111000`, `lngDelta = radius / (111000 * cos(lat))`) then apply precise Haversine in-memory and sort by distance.
  - Patterns: GeoJSON FeatureCollection: use `Prisma.JsonNull` (not plain `null`) in `geojson: { not: Prisma.JsonNull }` filter — plain null is not a valid `InputJsonValue`.
  - Patterns: Use `Prisma.AssetWhereInput` and `Prisma.GisLocationWhereInput` typed where objects instead of `Record<string, unknown>` when spreading into findMany to avoid include inference issues.
  - Patterns: Named `include` constant doesn't infer asset include; use inline `include` in the findMany call for typed result access.
  - Traps: The `near` static segment conflicts with `[id]` dynamic segment — since Next.js App Router resolves static segments first, `near/route.ts` is correctly matched before `[id]/route.ts` for `/api/gis-locations/near` requests.

---

## Iteration 37 — US-041: Contract management UI on asset detail page
- What was done: Added a "Contracts" tab to the asset detail page (`src/app/assets/[id]/page.tsx`). Tab renders a full list of contracts with contract reference, type badge, counterparty name, status badge (color-coded), start/end dates, and contract value. Contracts expiring within 90 days get an amber "Expiring soon" badge; expired contracts (past endDate) get a red "Expired" badge — computed client-side by comparing `endDate` against `now` and `now + 90 days`. "Add Contract" button opens a full-field modal (contract type, counterparty name/type, start/end date, contract value, periodic amount, payment frequency, renewal options, notice period, notes). Each contract row has Edit (opens pre-filled modal, calls PUT /api/contracts/:id), Renew (opens pre-filled start/end date modal, calls POST /api/contracts/:id/renew), and View Docs (Link to /documents page) actions. History sub-row: clicking "History" button fetches all contracts for the asset and filters those whose reference starts with the same base prefix (strips `-R\d+` suffix) and have `status=renewed`; shown as collapsible rows below the contract. Also created `src/app/api/contract-types/route.ts` — GET endpoint returning all active ContractType records ordered by name. Ran `tsc --noEmit` — clean.
- Files affected: `app/src/app/assets/[id]/page.tsx` (updated), `app/src/app/api/contract-types/route.ts` (new)
- Learnings for next cycles:
  - Patterns: Component function defined inside an IIFE tab renderer (`activeTab === "contracts" && (() => { ... function ContractFormFields(...) {...} return <div>...</div>; })()`); this keeps the sub-component co-located without creating a top-level component that would need props drilling.
  - Patterns: Expiry badge computed client-side with `new Date()` and `in90 = now + 90 days`; compare `endDate < now` for expired (red) and `endDate <= in90` for expiring soon (amber).
  - Patterns: Contract history computed from full asset contracts list by matching reference base prefix; strips trailing `-R\d+` to find the renewal chain without a dedicated "parent contract" FK.
  - Patterns: Renew modal pre-fills `renewStartDate` from `c.endDate` (the current contract's end date becomes the new start).
  - Patterns: A GET /api/contract-types endpoint was needed for the form select; created alongside the UI work rather than as a separate iteration.

---

## Iteration 38 — US-041 completion: Verify changes work in browser
- What was done: Marked the final acceptance criterion "Verify changes work in browser" as complete for US-041. All implementation criteria (Contracts tab, list rendering, expiry badges, Add/Edit/Renew/View Docs actions, history sub-row) were verified to be in place. TypeScript check (`tsc --noEmit`) passed cleanly.
- Files affected: `PRD.md` (checkbox marked)
- Learnings for next cycles:
  - Next task: US-042 — Work Order management UI (route `/work-orders`, Work Orders tab on asset detail page, status pipeline chips, create form).

---

## Iteration 39 — US-042: Work Order management UI
- What was done: Implemented the full Work Order management UI.
  1. Created `/api/work-order-categories` GET endpoint (`app/src/app/api/work-order-categories/route.ts`) returning all active categories.
  2. Created `/work-orders` page (`app/src/app/work-orders/page.tsx`) with full work order list, filters (status, priority, category, overdue toggle), pagination, "Create Work Order" modal, and work order detail modal with status pipeline chips.
  3. Added "Work Orders" tab to asset detail page (`app/src/app/assets/[id]/page.tsx`): new interfaces (AssetWorkOrder, CreateWOForm, WorkOrderCategoryRef), state variables, fetchAssetWorkOrders/fetchWoCategories/handleCreateWOSubmit callbacks, lazy loading on tab switch, list with priority/status badges and overdue indicator, status pipeline (Open → Assigned → In Progress → Pending Approval → Closed), Create WO modal (asset pre-filled), and detail modal.
  4. Contractor restriction is handled by the existing API RBAC (only returns work orders assigned to the contractor's user ID).
  5. TypeScript check (`tsc --noEmit`) passed cleanly.
- Files affected: `app/src/app/api/work-order-categories/route.ts` (new), `app/src/app/work-orders/page.tsx` (new), `app/src/app/assets/[id]/page.tsx` (modified), `PRD.md`
- Learnings for next cycles:
  - Pattern: `as const` array in tab navigation — add new tab value to the tuple for TypeScript to accept it.
  - Pattern: Inner IIFE `{activeTab === "tab" && (() => { ... })()}` works well for defining local helper functions per tab without polluting component scope.
  - Next task: US-043 — Condition Record UI on asset detail page.

---

## Iteration 40 — US-043: Condition Record UI on asset detail page
- What was done: Added a "Condition" tab to the asset detail page (`src/app/assets/[id]/page.tsx`). Added `ConditionRecord` and `RecordInspectionForm` interfaces. Added condition state variables (conditionRecords, currentCondition, conditionLoading/error, showRecordInspection, recordInspectionForm/errors/submitting/generalError). Added `fetchConditionRecords` callback (fetches from GET /api/assets/:id/condition-records, sets currentCondition to first record) and `handleRecordInspectionSubmit` callback. Updated `activeTab` type to include "condition". Updated `useEffect` to call `fetchConditionRecords` on tab switch. Updated tab navigation array and label mapping. Added a red safety banner at the top of the page (after the placeholder body banner) that shows when `currentCondition.safetyCondition === "unsafe" || "major_hazard"`. Added Condition tab content via IIFE pattern: prominently styled score card (color-coded 5=green→1=red) with all condition fields, timeline history list for prior records, and a "Record Inspection" modal with all fields (date, score select, structural/safety/maintenance/replacement selects, next inspection due, notes). TypeScript check passed cleanly.
- Files affected: `app/src/app/assets/[id]/page.tsx` (modified)
- Learnings for next cycles:
  - Patterns: Safety banner stored in `currentCondition` state (first record from condition-records endpoint); lazy-loaded when Condition tab is first activated — no extra API call at page load.
  - Patterns: Score color coding via `SCORE_COLORS: Record<number, string>` map inside the IIFE; avoids polluting component scope.
  - Patterns: Timeline history via `conditionRecords.slice(1)` — first record is current condition displayed in card, rest shown in timeline.
  - Next task: US-043 "Verify changes work in browser" then US-044 — Handover Record UI.
